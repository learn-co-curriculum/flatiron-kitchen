<%= form_for @recipe do |f| %>
  <%= f.text_field :name %>

  <% @ingredients.each do |ingredient| %>
    <div>
      <%= f.fields_for :recipe_ingredients, RecipeIngredient.new do |ri_fields| %>
        <%= ri_fields.label :name, ingredient.name %>
        <%= ri_fields.text_field :quantity %>
        <%= ri_fields.check_box :ingredient_id, {}, ingredient.id, nil %>
      <% end %>
    </div>
  <% end %>

  <%= f.submit %>
<% end %>
